---
const links = [
	{
		href: "/",
		label: "Listado",
	},
	{
		href: "/pokemons/1",
		label: "Paginado",
	},
	{
		href: "/favorites",
		label: "Favoritos",
	},
	{
		href: "/islas",
		label: "Islas",
	}
];

const currentPath = Astro.url.pathname;
// Astro.url es un objeto que contiene información sobre la URL actual, como el pathname, el host, el protocolo, etc. En este caso, estamos utilizando el pathname para obtener la ruta actual de la página. Esto es útil para determinar qué enlace debe estar activo en la barra de navegación.

---

<nav class="shadow bg-slate-900">
	<div
		class="container flex items-center justify-center p-6 mx-auto text-gray-600 capitalize dark:text-gray-300"
	>
		<!-- {
    Mi manera
            links.map(({ href, label }) => (
                <a
                    href={href}
                    class:list={[
                        "text-gray-800 dark:text-gray-200 border-b-2 mx-1.5 sm:mx-6",
                        {
                            "border-transparent": currentPath !== href,
                        },
                        {
                            "border-blue-500": currentPath === href,
                        }
                    ]}
                >   
                    {label}
                </a>
            ))
        } -->
		{
            // la manera del profe
			links.map(({ href, label }) => (
				<div>
					<a
						href={href}
						class="text-gray-800 dark:text-gray-200 mx-1.5 sm:mx-6"
					>
						{label}
					</a>
                    {
                        currentPath === href ? (
                            <div
                                transition:name="menu-line"
                                class="border-b-2 border-blue-500 mx-4"></div>
                        ) : (
                            <div class="border-b-2 border-transparent mx-4"></div>
                        )
                    }
				</div>
			))
		}
	</div>
</nav>
